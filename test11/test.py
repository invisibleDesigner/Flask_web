import uuid
import redis


# r = redis.Redis(host='127.0.0.1', port=6379, db=0, decode_responses=True)
# r.set('csrf_token', str(uuid.uuid4()))
# print(r.get('csrf_token'))

import uuid

print(uuid.uuid1())
print(uuid.uuid1())
print(uuid.uuid1())

